import React from 'react';
import styled from 'styled-components';
import { Text } from '../components/SS';
import Layout from '../components/Layout';

function IndexPage() {
	return (
		<Layout>
			<Text fontSize={5}>Big Text</Text>
		</Layout>
	);
}

export default IndexPage;
